# gckanbun: Issue #3 デバッグ方針書

対象: `ISSUE_BACKLOG.md` #3「重なり・座標のアサーションが無い」（P1、v2.6.0 時点で唯一残る P1）
作成: 2026-08-11 / 改訂: 2026-08-11（Codex レビュー反映）
engine: LuaLaTeX のみ（upLaTeX は方針どおり scope 外）

---

## 1. 何が問題か

現在の回帰アサーションは `\AssertSameWidth` / `\AssertSameMetrics` の2つで、
**2つの箱の `\wd` / `\ht` / `\dp` が一致するか**しか見ていない。

元の不具合は「返り点の**全体幅は期待どおり**なのに親字に視覚的に重なっていた」もので、
箱の外形は正しいまま中身の位置だけがずれていた。外形しか見ないアサーションは
この種のずれを構造的に検出できない。

不足しているのは精度ではなく**観測量**である。箱の寸法ではなく
**箱の中でグリフがどこに置かれたか**を見る必要がある。

## 2. 設計上の落とし穴

### 2.1 「重なったら失敗」は使えない

漢文組版では重なりが正常な場合が多数ある。送り仮名・返り点は負のカーンで意図的に重ね、
特殊返り点は `\makebox[0pt][r]` で幅ゼロの箱に入れ、ルビは親字の上に載るので
x 範囲は完全に重なるのが正常。`intrusion=pre|post|both` は隣接領域への食い込みが
機能そのものである。

判定は「重なりの有無」ではなく**「記録済みの正常な配置からの逸脱」**とする。

### 2.2 得られるのは箱ローカル座標であって最終ページ座標ではない

`\sbox` に詰めた箱を走査して分かるのは、**その箱を原点とする相対配置**だけである。
最終的なページ上の位置は行分割・ページ構築・LuaTeX-ja の方向変換を経て決まる。
本方針の検査対象は箱の内部配置に限定する。改行位置・行頭行末・ページ跨ぎで
のみ現れる欠陥は対象外（既存の目視マトリクスが引き続き担当する）。

### 2.3 グリフの寸法はインク境界ではない

glyph node の `width` は advance width であって字形の輪郭ではない。
アウトラインが advance を越えて張り出すことも、逆に余白を持つこともある。
よって本機構は**「既知の配置からのずれ」を検出する**ものであり、
「視覚的な重なり一般を検出する」ものではない。完了条件をこの範囲に合わせる。
`xoffset` / `yoffset` は座標に加算すること。

### 2.4 縦組はノードの `dir` からは判定できない

LuaTeX-ja の縦組は LuaTeX ネイティブの direction ではなく、箱の回転として
実装されている。ノードの `dir` フィールドを見ても `\tate` を復元できない。

一方、**このパッケージは既に TeX 側で方向を持っている**
（`\iftdir` と `\l__gckanbun_tdir_bool`、`gckanbun.sty:146-159`）。
したがって推測せず、**TeX 側から Lua へ方向を引数として渡す**。

### 2.5 伸縮 glue の実効値には親箱が要る

本体は `\hbox to <dim>{... \hss ...}` を12箇所で使っている
（`gckanbun.sty:501-515` ほか）。glue node 単体では実効幅が決まらず、
**直近の親 hlist/vlist の `glue_set` / `glue_sign` / `glue_order`** が必要。
再帰時に親箱を引き回すこと。

### 2.6 全グリフのスナップショットは過剰かつ脆い

見た目が同じままでも、LuaTeX-ja や jlreq の更新でノードの入れ子・glue・
補助グリフが変わると大量の差分が出る。検査対象は
**役割を付けたグリフ間の相対座標**（親字と返り点、親字と送り仮名など）に絞る。
対応付けは出現順の一致ではなく、属性による役割タグ＋階層パス＋出現番号で行う。
font は数値 ID が読み込み順で変わるため、ファイル名等に正規化する。

## 3. 方針

保存箱の中身を Lua で走査し、役割タグ付きグリフの**箱ローカル座標**を算出して、
記録済みの基準値（golden）と比較する。

**却下: 画像比較** — `ISSUE_BACKLOG.md` #8 が未解決でテストが macOS の
ヒラギノに依存している。環境が変わればグリフ形状そのものが変わるため、
ピクセル比較は #8 を先に解決しない限り偽陽性を量産する。

## 4. タスク分割

Codex レビューを受けて順序を変更した。**まず欠陥を観測できることを確かめてから
機構を作る**（機構を先に作ると、肝心の欠陥が観測できない機構が完成しうる）。

### T1: テスト環境の明示と記録 【縮小】
当初「テストが Hiragino 依存」と想定していたが**誤り**だった。実測の結果、
`gckanbun-edge-test.tex` はフォントを一切指定しておらず jlreq 既定を使い、
PDF に埋め込まれるのは `HaranoAjiMincho-Regular` と Latin Modern、
フォント代替警告は 0 件だった。#8 は**ドキュメント本体だけの問題**である。

したがって T1 は以下に縮小する:
- jlreq 既定への暗黙の依存を明示指定に変える（既定が変わっても壊れないように）
- golden と併せて記録する環境情報: TeX Live 版、LuaTeX 版、LuaTeX-ja 版、
  luaotfload 版、jlreq 版、HaranoAji 版
- **検証**: `make check` が成功し、フォント代替警告が 0 のまま

### T2: 観測量の特定（スパイク）【完了 2026-08-11】

**結論: 関門を通過。位置アサーションは幅アサーションが原理的に見られない
欠陥を捉えられることを実証した。**

#### 判明したこと

1. **履歴からの再現は不発**。v2.4.0〜v2.6.0 の 9 版すべてで
   `gckanbun.sty` を差し替えてプローブをビルドしたが、モノルビ・送り仮名・
   返り点・句読点・特殊返り点・`\KanHyphen` の 8 ケースで**全版が
   バイト単位で同一の座標**を出した。この範囲の変更はグループルビ経路に
   集中している。

2. **グループルビ経路の欠陥は既存アサーションで検出できる**。
   `2c3710d`（v2.5.1→v2.5.2「Fix group return-mark spacing」）が入れた
   `\g__gckanbun_group_follow_kaeri_shift_dim` は「ルビが親文字列より突出し、
   かつ intrusion が post/both でない」場合にのみ発火する。突出するケース
   （`\グ振り{天}{てんちじん}\返り{レ}書`）で旧新を比較すると
   箱幅が 2359296 → 1916928 と変わる。つまり `\AssertSameWidth` で捕まる。

3. **本当の盲点は幅ゼロ箱の中**。特殊返り点（`\IchiRe` / `\JyouRe` /
   `\KouRe` / `\TenRe`）は `\raisebox{...}{一}\makebox[0pt][r]{レ}` の形で
   レを重ねている。`\makebox[0pt][r]` は幅ゼロなので、**その中身の位置ずれは
   定義上どうやっても外箱の幅に現れない**。

#### 実証（`\IchiRe`、横組、jlreq 9pt、HaranoAjiMincho）

`\makebox[0pt][r]{レ}` を `\makebox[0pt][r]{レ\kern0.1\zw}` に変えて
`天\IchiRe 地` を組み直した結果:

| 観測量 | 基準 | 摂動後 | 判定 |
|---|---|---|---|
| 箱 (w/h/d) | 1769472 / 690090 / 70779 | 1769472 / 690090 / 70779 | **完全一致** |
| レ (U+30EC) の x | 589824 | 530838 | **−58986 sp** |
| 天・一・地 の座標 | — | 変化なし | 一致 |

親字「天」は x∈[0, 589824] を占めるので、基準ではレの左端が親字の右端に
接していたのが、摂動後は 58986 sp（=0.1zw）食い込む。これは #3 が記述する
「返り点が親字に重なるが全体幅は期待どおり」そのものであり、
`\AssertSameWidth` と `\AssertSameMetrics` は**両方 PASS してしまう**。

#### 採用する観測量

- 幅ゼロ箱（`\makebox[0pt][r]`）内グリフの箱ローカル x / y
- ルビ vbox 内グリフの箱ローカル x / y
- 単位は sp のまま。上記の差は 58986 sp と大きく、丸めの心配はない

#### ダンパの妥当性の裏付け

最初の摂動ではレの**前**に kern を入れたが座標が動かなかった。これは
右揃え箱では先行 kern が `\hss` に吸収されるためで、**正しい挙動**である。
glue の実効値を親箱の `glue_set` 経由で取れていることの裏付けになった。

#### T3 への引き継ぎ

スパイクのダンパは `scratchpad` にあり、リポジトリには入れていない。
T3 で作り直す際、以下は実証済みなので踏襲してよい:
- `node.effective_glue(glue, parent)` に直近の親箱を渡す方式
- hlist は幅を x に、vlist は height+depth を y に累積し、
  vlist の子は x を進めない
- hlist 内の `shift` は y に負で、vlist 内の `shift` は x に正で効く

未検証（T3 で確認が必要）: 縦組、`\hbox to` の伸長（縮小のみ実証）、
深い入れ子での符号。

### T3: 座標抽出ヘルパ 【完了 2026-08-11】

**成果物**: `gckanbun-nodepos.lua`（本体）、
`gckanbun-nodepos-selftest.lua`（判定ロジック）、
`gckanbun-nodepos-selftest.tex` / `-tate.tex`（検査ケース）。
いずれもテスト専用で `gckanbun.sty` からは読み込まれない。
ノードリストは読むだけで書き換えないため、組版には一切影響しない。

**検査結果**: 横組 9/9 PASS、縦組 4/4 PASS。両方向とも期待値を
**1 sp** ずらすと FAIL し終了コード 1 を返すことを確認済み。

T2 で未検証としていた 3 点はすべて解消した:

- `\hbox to` の**伸長**: `\hbox to 3\zw{天\hfil 地}` で地が 2zw に来ることを確認
- 深い入れ子: `\hbox to 5\zw{\hbox{天地}\hfil}` で内箱の内部座標が
  外箱の伸長に影響されないことを確認
- 縦組: 下記のとおり**予測が外れ、設計を変更した**

#### 縦組についての発見（重要）

当初「箱ローカル座標は横組と同じ値になる」と予測したが**外れた**。
縦組では LuaTeX-ja がグリフをもう 1 階層深い箱に包み、その包み箱が
一律のオフセットを持つ（横組比で x に -0.5zw、y にも定数）。
箱自体も h = d = 0.5zw の縦組メトリックになる。
グリフのパスも横組の `0.3` に対し縦組は `0.2h.2h.4` と 1 段深い。

一方で**グリフ間の距離は横組と完全に一致する**。

したがって:

- **絶対座標は横組と縦組で比較できない**。golden はケースごと・方向ごとに
  別レコードとして持つ
- **相対座標（役割グリフ間の距離）は方向に依らず安定**。手計算で期待値を
  書けるのも相対座標の方
- 自己検査は横組を絶対座標、縦組を相対座標で書いてある

これは Codex レビューの「相対座標を見るべき」という指摘と独立に一致した。
T4 の比較コアは**相対座標を第一級で扱う**こと。

#### 実装上の決定

- 対応付けは `(文字コード, その文字の何個目か)` で行い、キーは `U+5929/1` の形。
  区切りに `#` を使わないのは、TeX のマクロ引数と Lua の文字列の両方で
  エスケープを要求されて衝突するため（実際に踏んだ）
- **属性によるタグ付けは採用しない**。LuaTeX-ja が属性を多用しており、
  テスト側で属性を置くと組版そのものに干渉しうる
- 階層パスは診断表示のみに使い、比較には使わない

#### 作業手順についての注意

`make distclean && make check` は**追跡済み PDF を壊す**。`check` が
再生成しない `gckanbun-doc.pdf` / `gckanbun-sample.pdf` /
`gckanbun-edge-test-uptex.pdf` の 3 つが削除されたまま残り、
再生成される 4 つもバイトが変わる（内容は同一でメタデータのみの差）。
T4 以降も、繰り返しビルドする作業は `git clone --local` した複製で行い、
検証済みのソースだけを本物へ持ち込むこと。作業前に追跡済み PDF の
md5 を控え、作業後に突き合わせる。

### T3: 座標抽出ヘルパ（2次元・方向対応を最初から含める）
- `gckanbun-nodepos.lua`（テスト専用。パッケージ本体には入れない）
- 入力: 箱番号と、TeX 側から渡された方向フラグ
- 出力: 役割タグ付きグリフの `{role, path, index, char, x, y, w, h, d, font}`
- hlist は進行方向に幅を、vlist は高さ・深さを累積する。子箱の `shift` は
  進行方向と直交する変位として扱う。親箱の glue_set 等を再帰に引き回す
- `xoffset` / `yoffset` を座標に加算する
- **検証**: 座標が既知の小ケースで先に確かめる。最低限
  「伸縮 glue を含む `\hbox to`」「hlist→vlist→hlist の入れ子」
  「`\raisebox` による shift」「負の kern」「横組と縦組」
  （`\hbox{あい}` だけでは実装の難所を一切検査できない）
- **成果物**: `gckanbun-nodepos.lua` と単体確認用 tex

### T4: 比較コア
- 役割タグ付き座標列を2つ受け取り差分を返す純関数
- 単位は sp のまま扱う。許容差は既定 0 （完全一致）、必要な箇所だけ数 sp
- 差分は「どの役割のグリフが期待値から何 sp ずれたか」を返す
- **検証**: 関数単体のテスト（golden ファイル入出力を経由しない）
- **成果物**: 比較関数とその単体テスト

### T5: golden の入出力と TeX 連携
- `\AssertGlyphLayout{<name>}{<content>}`
- **golden が無ければ `make check` は必ず失敗する**。自動生成はしない
  （初回自動書き出しは、基準値の欠落やファイル名の誤りを正常な更新として
  自己承認してしまう）
- 更新は明示的な別ターゲット `make update-golden` に分離する
- 内容は**一度だけ**箱詰めし、その同じ箱を検査する。複数回組むと
  可変グローバル状態（`ISSUE_BACKLOG.md` #9）や lookahead の影響で
  比較機構自身がレイアウトを変えうる
- ケース間でグローバル状態が初期化されることを確認する
- **検証**: `gckanbun.sty` の配置数値を**実際に起こりうる小ささ**でずらして
  FAIL し、戻して PASS すること（1pt は粗すぎる。数 sp〜0.1pt 程度で試す）

### T6: 元の欠陥ケースの回帰追加と `make check` 組み込み
- T2 で特定した最小ケースを T5 の形式で追加
- 旧コードで FAIL、現行コードで PASS を確認
- `Makefile` の依存に `gckanbun-nodepos.lua` と golden ファイルを含める
  （含めないとヘルパ変更後にテストが再実行されない）
- 既存の `AssertSameWidth` ケースは**移行しない**。外形の等価性を見たい意図が
  明確なものはそのまま残す。段落・縦組の目視マトリクスも別目的として残す
- **検証**: `make distclean && make -j check`

## 5. やらないこと

- パッケージ本体（`gckanbun.sty`）の挙動変更。本方針は検出機構の追加のみ
- upLaTeX 対応（scope 外）
- ドキュメント本体のフォント変更（#8 として別途）
- CI 化（#12 として別途。ただし T6 まででローカル完結させ、CI からは
  `make check` を呼ぶだけで済む形にする）
- 改行・ページ跨ぎ・実際の縦組回転後にのみ現れる欠陥の検出（2.2 参照）

## 6. 完了条件

1. `make check` が新旧すべてのアサーションを実行して成功する
2. `gckanbun.sty` の配置に関わる数値を数 sp ずらすと `make check` が失敗する
3. 元の重なり欠陥を再現するコードに対して失敗する回帰ケースが存在する
4. golden が無い状態では `make check` が失敗し、更新は明示コマンドを要する
5. golden に生成環境の版情報が併記されている

（「視覚的な重なりを一般に検出できること」は完了条件に**しない**。2.3 参照）
